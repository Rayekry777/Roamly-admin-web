# Roamly 管理 Web 契约

```yaml
version: 9
updatedAt: 2026-09-12
scope: 平台账号、审核、治理、财务页面与前端工程
reviewStatus: accepted
designStatus: 已冻结
implementationStatus: 已实现（Demo 范围）
projectStatus: 阶段 41 已实现
frameworkBaseline: plus-ui v5.6.2-v2.6.2
referenceCommit: d0d451967676707021b9857df529c395b27e90a7
```

## 契约职责

本文只管理 `Roamly-admin-web` 的管理页面、客户端权限、请求适配和验收。HTTP 线协议由后端运行时 OpenAPI 管理，阶段顺序参考 [四端交付路线图](../Roamly/docs/roadmap/FOUR_END_DELIVERY_ROADMAP.md)。本文不定义数据库、服务端事务、消费者页面或商户小程序流程。

## 工程基线

- 固定参考 `JavaLionLi/plus-ui v5.6.2-v2.6.2` 提交 `d0d451967676707021b9857df529c395b27e90a7`，保留 MIT License 和第三方声明。
- 锁定 Node 24、pnpm 11、Vue 3.5.30、TypeScript 5.9、Vite 7.3.2、Vue Router 5.0.3、Pinia 3.0.4、Element Plus 2.13.5、Axios 1.13.6、ECharts 6 和 Vitest 4.0.18。
- 使用静态路由与后端权限集合过滤菜单；不接入若依动态路由、数字响应码或 `sys_*` 模型。
- 业务 ID 保持字符串，金额由分格式化，时间显示 Asia/Shanghai。

## plus-ui 取舍

| 处理       | 能力                                                                                                                                                                       |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 保留并重做 | 布局、侧栏折叠、面包屑、标签页、路由守卫、Pinia、权限指令、分页、列显隐、筛选重置、表单弹窗、图片上传/预览、下载和 ECharts                                                 |
| 完全改写   | 登录页、首页、Axios 请求层、用户 Store、静态菜单、错误提示、主题变量和业务页面                                                                                             |
| 已接入     | 管理 SSE 资源刷新、自有同步 XLSX 导出（Fesod 兼容边界）；WarmFlow 待办入口与运维系统外链保留为扩展                                                                         |
| 删除       | 若依品牌、默认蓝色、主题色选择器、租户、部门岗位、动态菜单、通用字典、通知中心、OSS 管理、监控页面、代码生成、工作流设计器、社交登录、SSE 演示、请求加密、Quill 和演示页面 |

本轮不引入 `vxe-table`，所有业务表格统一使用 Element Plus，避免维护两套列、筛选和选中状态。

## Roamly 视觉

- 固定使用 `#ff5f57` 主色、`#e94f49` 悬停色、`#8275ff` 辅助色、`#242331` 主文字、`#8f8d99` 辅助文字、`#f4f5fb` 背景、`#ffffff` 内容面和 `#ececf3` 边框。
- 管理端将消费者端风格转换为高密度工作台：内容圆角 8px、控件圆角 6px、细边框和柔和紫灰阴影；不复制移动端大圆角。
- 采用固定侧栏、顶部上下文区、标签页、紧凑筛选、表格和抽屉详情；页面分区不做悬浮卡片堆叠，禁止卡片嵌套。
- 主操作使用珊瑚红，次操作白底描边，危险操作使用独立危险色；状态不能全部染成品牌色。
- 图标优先使用 Element Plus/Lucide；竞品截图只参考信息结构，不复制任何视觉资产。
- 不提供主题颜色选择器；所有页面、空态、登录页和图表必须使用 Roamly 品牌。

## 登录与权限

- 登录调用 `/v1/admin/auth/login`，Token 使用独立 `ADMIN`（管理端）存储键；401 清理会话并回到登录页，403 保留当前页面并提示权限不足。
- 强制改密期间只允许访问改密和退出；改密成功后清理本地会话并重新登录。
- `PLATFORM_ADMIN`（平台超级管理员）管理账号并拥有全部业务权限；`MERCHANT_REVIEWER`（商户审核员）处理商户和券审核；`FINANCE`（财务管理员）处理退款、佣金与结算。
- 管理员账号支持新增、编辑、启用、停用和重置密码，不提供删除、用户名修改、角色编辑器或菜单编辑器。
- 菜单和按钮权限来自 `/v1/admin/auth/me`，前端权限指令只控制可见性，不替代服务端授权。

## 信息架构

| 路由                     | 页面       | 所需权限                                            | 可见角色                           |
| ------------------------ | ---------- | --------------------------------------------------- | ---------------------------------- |
| `/login`                 | 登录       | 公开                                                | 公开                               |
| `/dashboard`             | 运营摘要   | `admin:dashboard:read`（运营摘要查看）              | 全部管理员                         |
| `/admin-users`           | 管理员账号 | `admin:user:manage`（管理员账号管理）               | `PLATFORM_ADMIN`（平台超级管理员） |
| `/merchant-applications` | 商户申请   | `admin:merchant-application:review`（商户申请审核） | 平台超级管理员、商户审核员         |
| `/shops`                 | 门店治理   | `admin:shop:govern`（门店治理）                     | 平台超级管理员、商户审核员         |
| `/voucher-reviews`       | 券审核     | `admin:voucher:review`（团购券审核）                | 平台超级管理员、商户审核员         |
| `/orders`                | 订单和支付 | `admin:trade:read`（订单、支付与核销查看）          | 平台超级管理员、财务管理员         |
| `/refunds`               | 退款处理   | `admin:refund:manage`（退款处理）                   | 平台超级管理员、财务管理员         |
| `/redemptions`           | 核销与撤销 | `admin:trade:read`（订单、支付与核销查看）          | 平台超级管理员、财务管理员         |
| `/commission-rules`      | 佣金规则   | `admin:commission:manage`（佣金规则与资金账本管理） | 平台超级管理员、财务管理员         |
| `/ledger-entries`        | 资金账本   | `admin:commission:manage`（佣金规则与资金账本管理） | 平台超级管理员、财务管理员         |
| `/settlements`           | 结算批次   | `admin:settlement:manage`（结算处理与失败重试）     | 平台超级管理员、财务管理员         |
| `/audit-logs`            | 操作审计   | `admin:audit:read`（操作审计查看）                  | `PLATFORM_ADMIN`（平台超级管理员） |

## 页面行为

- 账号页覆盖创建、编辑、启停、密码重置、乐观锁冲突和最后平台管理员保护反馈。
- 商户和券审核页使用列表加抽屉/详情页，展示结构化资料、历史决定、通过与驳回；驳回原因必填。
- `/voucher-reviews` 以审核状态、券型、门店 ID 和关键词筛选，详情展示真实门店、券型、价格库存、有效期、时段、套餐明细、预约/退款规则和媒体摘要；`PENDING`（审核中）才显示通过与驳回，命令携带 `Idempotency-Key`（幂等键），409 时重新加载服务端版本。
- 订单、退款、核销、账本、结算和审计页面提供紧凑筛选、分页、列显隐、固定操作列和导出。
- 退款处理页支持平台直接发起退款、审批商户/消费者申请、驳回和失败重试；金额由服务端按订单券明细计算，成功后账本追加退款冲回分录。
- 财务操作提交期间禁用重复点击并发送 `Idempotency-Key`；409 后重新加载服务端事实，不能用本地数据覆盖。
- SSE 只通知资源 ID 和事件类型，页面收到后显示刷新提示或静默刷新；断线回退为定时查询。
- 所有状态码使用中文标签，例如 `PROCESSING`（处理中）、`SUCCEEDED`（成功）、`FAILED`（失败）。

## 阶段 41 退款与客服工作台

- 退款工作台按审核状态与执行状态分别形成待审核、等待退款、处理中、失败、人工处理、部分退款、已完成和已拒绝队列；详情展示逐券金额、失败原因、Mock 渠道流水与服务端时间线。
- 客服工作台采用队列、会话和业务上下文三栏布局，服务端队列覆盖待认领、我的、超时、高优先级和退款工单；新消息、消费者和商户维度在客户端基于权限范围内结果展示。
- 客服操作覆盖认领、公开回复、内部备注、附件、快捷回复、标签、转交和状态白名单；未认领、已解决和已关闭工单不展示回复与转交入口。
- SSE 只触发权威数据重新查询，断开、连接和重连不修改退款或客服状态。
- 本阶段全部业务 ID 保持字符串；前端只提交操作命令，不能直接写退款审核、执行或客服最终状态。

## 阶段 19 管理 Web 冻结设计

阶段 19 完整服务端线协议以运行时 OpenAPI 与后端 [商户审核和门店治理详细设计](../Roamly/docs/stages/STAGE_19_MERCHANT_REVIEW_AND_SHOP_GOVERNANCE.md) 为准；本节只管理本端信息结构、交互、客户端状态和自动化标准，当前能力已实现。

### 商户申请页

- `/merchant-applications` 使用紧凑筛选、分页表格和最大 760px 详情抽屉。筛选包含状态、城市、类目、精确手机号、提交时间范围、查询与重置。
- 表格展示申请编号、门店名、类目、城市、联系人、脱敏手机号、提交时间、中文状态和操作，不在列表阶段读取或缓存私有图片。
- 抽屉按审核摘要、主体证照、门店地址、七日营业时间、经营图片、Mock 结算和审核记录分区；不把多个页面分区包装成嵌套卡片。
- 私有图片通过带 `ADMIN`（管理端）Bearer Token 的 Blob 请求获取，不直接绑定永久 URL；关闭抽屉、切换申请和组件卸载时必须调用 `URL.revokeObjectURL`。
- 仅 `PENDING`（审核中）显示通过与驳回。通过确认必须明确“通过后立即激活门店”；驳回原因去首尾空格后 1 至 500 字。
- 通过或驳回首次点击生成 8 至 128 字符 `Idempotency-Key`；网络重试复用原键，用户明确修改命令参数后生成新键。提交期间按钮与关闭动作进入稳定加载状态。
- 409 冲突不清空驳回原因或关闭弹窗；重新加载详情和列表后，提示管理员按服务端最新版本重新核对。

### 门店治理页

- `/shops` 使用状态、城市、类目与关键词筛选；分页表格展示门店、租户、类目、城市、账号摘要、经营状态、最近变更和停用原因。
- 详情抽屉展示来源申请、门店地址、七日营业时间、租户、各账号状态数量和最近治理信息。
- 仅 `ACTIVE`（营业中）显示停用，仅 `SUSPENDED`（已停用）显示恢复；停用与恢复均使用确认弹窗并要求 1 至 500 字原因。
- 恢复确认提示只恢复因 `SHOP_SUSPENSION`（门店停用联动）而停用的账号，且不会自动恢复后续阶段已下架的券。
- 治理命令的幂等键生命周期和 409 处理与审核命令一致；成功后刷新门店列表、详情和运营摘要，不本地推测联动账号状态。

### 客户端模型与请求

- 新增申请列表项、申请详情、审核结果、门店列表项、门店详情、治理结果和嵌套营业时间/媒体/账号摘要类型；全部业务 ID 使用 `string`。
- 查询参数只发送已填写字段；日期时间转换为 ISO-8601，页码从 1 开始，页大小限制 1 至 100。
- 图片接口采用独立二进制请求方法，JSON 错误仍解析为现有字符串错误码；401 清理会话，403 保留页面，404 关闭失效详情，409 走资源刷新，503 显示对象存储暂不可用。
- 状态字典必须覆盖 `DRAFT`（草稿）、`PENDING`（审核中）、`APPROVED`（审核通过）、`REJECTED`（审核未通过）以及 `ACTIVE`（营业中）、`SUSPENDED`（已停用）、`CLOSED`（已关闭）；用户界面只展示中文文案。
- 菜单、路由和按钮分别消费 `admin:merchant-application:review`（商户申请审核）与 `admin:shop:govern`（门店治理），权限指令不替代请求层与后端授权。

### 视觉与响应式

- 沿用现有 Roamly 高密度管理风格：珊瑚红主操作、紫色辅助、紫灰文字、浅灰背景、白色内容面、8px 内容圆角与 6px 控件圆角。
- 1440x900 与 1280x720 保持筛选区、表格、分页和固定操作列可见，详情抽屉不超过 760px。
- 390x844 使用纵向筛选、可横向滚动表格和全屏抽屉；长门店名、原因、状态与弹窗按钮不得溢出或重叠。
- 不引入若依默认蓝色、主题切换器、竞品 Logo、移动端大圆角或营销式大标题。

### 本端验收

- Vitest 覆盖请求与类型适配、查询规范化、中文状态、权限按钮、Blob URL 创建/释放、幂等键重放、409 刷新和 401/403/404/503。
- Playwright 覆盖申请通过、申请驳回、私有图片、并发冲突、门店停用、选择性账号恢复以及平台管理员、审核员、财务管理员三类菜单。
- 执行 TypeScript、ESLint、Prettier、Vitest 和生产构建；生成并检查 1440x900、1280x720、390x844 三个视口截图。
- 阶段 19 已通过后端真实 HTTP、数据库事务、运行时 OpenAPI 与本端自动化联合验收。

## 请求与下载

- 仅调用 `/v1/admin/**`，使用真实 HTTP 状态、`Result/PageResult`、字符串 `code=OK`（成功）和 Bearer Token。
- Axios 不兼容若依数字码，不自动把 GET 参数拼接成自定义字符串，不实现前端 500ms 请求体判重，不启用 AES/RSA 请求加密。
- 401、403、404、409、429、503 使用独立全局处理；业务错误保留后端字符串错误码。
- XLSX 导出调用各集合的 `POST .../export`，请求体复用当前筛选条件；成功保存二进制，JSON 错误响应按统一错误结构显示。
- v8 收口：账本页 `/ledger-entries` 已提供带 `Download` 图标的导出按钮，调用 `POST /v1/admin/ledger/export`；导出期间按钮锁定，二进制成功保存为 `roamly-ledger.xlsx`，`EXPORT_RESOURCE_INVALID`（导出资源无效）和 `EXPORT_TOO_LARGE`（导出数据超限）等 JSON 错误沿用统一提示。

## 本端阶段

| 阶段 | 管理 Web 交付                                 | 状态                         |
| ---: | --------------------------------------------- | ---------------------------- |
|   15 | 独立仓库、plus-ui 裁剪、Roamly 主题和测试基座 | 已实现                       |
|   16 | 登录、强制改密、账号管理和基础仪表盘          | 已实现                       |
|   19 | 商户审核和门店治理                            | 已实现                       |
|   21 | 券审核和消费者视角预览                        | 已实现                       |
|   24 | 订单查询和财务异常退款                        | 已实现                       |
|   26 | 核销与撤销审计                                | 已实现                       |
|   27 | SSE 资源刷新                                  | 已实现                       |
|   28 | 佣金规则和账本                                | 已实现                       |
|   29 | 结算、失败重试、操作审计和 XLSX 导出          | 已实现                       |
|   30 | 单元、E2E 和视觉验收                          | 已实现（自动化；真机未确认） |
|   36 | 门店租户字段与租户文案                        | 已实现                       |
|   41 | 退款队列、退款时间线和客服三栏工作台          | 已实现                       |

## 验收

- Vitest 覆盖请求适配、路由守卫、静态菜单、权限指令、金额/时间格式、表格筛选、下载错误和 401/403/404/409/429/503。
- Playwright 覆盖登录、首次改密、管理员生命周期、商户/券审核、退款、核销查询、佣金、结算和导出。
- 执行 pnpm 锁文件安装、类型检查、Lint、单元测试和生产构建。
- 浏览器截图覆盖 1440x900、1280x720、390x844，检查表格、抽屉、按钮和长文本无重叠、溢出或布局跳动。

阶段 16 已完成：Vitest 4 个文件 16 项通过；Playwright 桌面与移动项目 7 项通过、1 项按项目条件跳过；三个冻结尺寸已生成截图并完成人工复核。

阶段 19 已完成：Prettier、TypeScript、ESLint、6 个 Vitest 文件 24 项和生产构建通过；Playwright 桌面与移动项目 16 项通过、2 项按项目条件跳过，桌面项目统一生成并人工复核 1440x900、1280x720、390x844 三个冻结尺寸截图。

2026-09-08 阶段 36：门店查询占位、表头、详情区、类型与 E2E fixture 已统一切换为租户语义和 `tenant*` 契约；`pnpm verify`、`pnpm format:check` 和生产构建通过，8 个 Vitest 文件共 28 项通过。

2026-09-12 阶段 41：退款双状态队列、逐券详情与时间线、客服三栏工作台、服务端队列筛选、游标消息、附件、快捷回复、标签及转交已落地；`pnpm verify`、`pnpm format:check` 和生产构建通过，9 个 Vitest 文件共 31 项通过。

## 非目标

本端不包含消费者页面、商户经营页面、动态 RBAC、菜单编辑、部门岗位、多租户、工作流设计、系统监控、真实支付后台和若依默认视觉。
